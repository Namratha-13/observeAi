# ── Postgres ──────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=observeai
POSTGRES_USER=observeai
POSTGRES_PASSWORD=changeme

# ── ClickHouse ────────────────────────────
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DB=observeai
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# ── Redis ─────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379

# ── Kafka ─────────────────────────────────
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC_RAW_TRACES=raw-traces
KAFKA_TOPIC_EVALUATED_TRACES=evaluated-traces
KAFKA_TOPIC_COST_EVENTS=cost-events
KAFKA_TOPIC_DLQ=dlq

# ── Ingest API ────────────────────────────
INGEST_API_PORT=8001
INGEST_API_SECRET_KEY=changeme-32-char-secret

# ── API Gateway ───────────────────────────
GATEWAY_PORT=3000

# ── LLM keys (for eval engine) ───────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...